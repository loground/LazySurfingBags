import React from "react";

export const Description = () => {
  return (
    <div className="jumbotron">
      <h1>Lazy Bags</h1>
      <p className="mainText">
        Sorry, we've been kinda way too lazy to make this description, so now it
        looks like this.
      </p>
      <p className="lazyText">Stay lazy!</p>
    </div>
  );
};

export default Description;
